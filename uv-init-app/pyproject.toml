[project]
name = "uv-init-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Demo User" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
uv-init-app = "uv_init_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
